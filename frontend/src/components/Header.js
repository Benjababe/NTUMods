const Header = () => {
    return (
        <div>
            <img
                className="img-logo"
                alt="Logo"
                src="/logo.png"></img>
        </div>
    );
}

export default Header;